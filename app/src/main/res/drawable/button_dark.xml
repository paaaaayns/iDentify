<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid
        android:color="@color/dark_teal" /> <!-- Change the button's background color as needed -->
    <corners
        android:radius="10dp" /> <!-- Adjust the radius to change the roundness -->
    <stroke
        android:width="2dp"
        android:color="@color/bg1" />

</shape>